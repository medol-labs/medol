# Resource Action Layout

- Create action: lifecycle-starting command.
- Edit action: command that changes core fields of one item.
- Delete/deactivate action: command with irreversible or status-changing effect.
- Item action: command scoped to one row/detail item.
- Background action: command that needs no form and can be triggered directly.

Keep generated layouts compact and predictable.
