# Automation Patterns

- Event processor: reacts directly to an event.
- Policy: evaluates domain conditions and issues commands.
- Saga/workflow: coordinates multiple events, commands, and external waits.
- Scheduler: triggers based on time.

Prefer the smallest pattern that matches the MEDOL slice.
